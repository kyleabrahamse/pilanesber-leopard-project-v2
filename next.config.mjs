/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["firebasestorage.googleapis.com", "www.spongebobshop.com", "m.media-amazon.com"],
  },
};

export default nextConfig;
